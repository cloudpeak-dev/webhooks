services:
  app:
    build:
      context: .
    ports:
      - "8084:8080"
    volumes:
      # Mount the private SSH key as read-only
      - ~/.ssh/id_rsa:/root/.ssh/id_rsa:ro
    environment:
      - WEBHOOK_KEY=${WEBHOOK_KEY}
